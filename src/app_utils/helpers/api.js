export const getRequest = url => {};

export const postRequest = (url, payload) => {
  return new Promise((resolve, reject) => {
    resolve("ok");
  });
};

export const putRequest = (url, payload) => {};

export const deletRequest = url => {};
